# 🧠 AI Intent Engine - ตัวอย่างเรียก API

## สถานการณ์

- **FastAPI** รันอยู่บนเครื่อง **IP:** `10.134.100.35`  
- **Port:** `8000`  
- **Endpoint:** `/intent`  
- **หน้าที่:** รับข้อความจากผู้ใช้ → ส่งไปหา AI → ได้ **เจตนา (intent)** กลับมา

---

## 💡 ตัวอย่าง 1 — เรียก API แบบง่าย (CLI / PHP Script)

        ```php
        <?php
        // ข้อความที่ผู้ใช้พิมพ์
        $text = "บ้านใกล้โรงเรียน งบไม่เกิน 2 ล้าน";

        // สร้าง URL พร้อม query string
        $url = "http://10.134.100.35:8000/intent?text=" . urlencode($text);

        // เริ่ม cURL
        $ch = curl_init($url);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
        curl_setopt($ch, CURLOPT_POST, true); // ใช้ POST
        curl_setopt($ch, CURLOPT_HTTPHEADER, [
            'accept: application/json'
        ]);
        curl_setopt($ch, CURLOPT_POSTFIELDS, ''); // body ว่าง

        // ส่ง request
        $response = curl_exec($ch);
        $err = curl_error($ch);
        curl_close($ch);

        // แสดงผล
        if ($err) {
            echo "เกิดข้อผิดพลาด: $err";
        } else {
            echo "<pre>";
            print_r(json_decode($response, true));
            echo "</pre>";
        }
        ?>


📌 ตัวอย่างผลลัพธ์ (Response จาก AI Intent Engine)

        Array
        (
            [text] => บ้านใกล้โรงเรียน งบไม่เกิน 2 ล้าน
            [embedding] => Array
                (
                    [0] => -0.016778355464339
                    [1] => 0.089780800044537
                    [2] => 0.07676275074482
                    [3] => 0.01098791975528
                    [4] => -0.061577577143908
                    [5] => 0.013065434992313
                    [6] => 0.026377791538835
                    [7] => 0.034065801650286
                    [8] => 0.046252351254225
                    [9] => -0.040635664016008
                )
            [keywords] => Array
                (
                    [0] => บ้านใกล้โรงเรียน
                    [1] => งบไม่เกิน
                    [2] => ล้าน
                )
            [intent] => ค้นหาบ้านราคาต่ำกว่า 2 ล้าน ใกล้โรงเรียน
        )


🔹 อธิบายแบบบ้าน ๆ

    text → ข้อความที่ผู้ใช้พิมพ์

    intent → AI บอกความตั้งใจหลัก → ระบบรู้ว่าต้องทำอะไรต่อ

    keywords → ช่วยกรองข้อมูล → เจอสิ่งตรงใจผู้ใช้

    embedding → ทำให้ระบบค้นหาแบบ “ฉลาด” เข้าใจความหมาย แม้ไม่ตรงคำ





🌐 Flow Diagram (ASCII / Markdown)

+-----------------+
| ผู้ใช้พิมพ์ข้อความ |
+-----------------+
          |
          v
+-----------------+
| ส่งข้อความไป AI  |
| Intent Engine   |
+-----------------+
          |
          v
+-----------------+
| AI วิเคราะห์     |
| - intent        |
| - keywords      |
| - embedding     |
+-----------------+
          |
          v
+-----------------+
| ระบบนำข้อมูลไปใช้ |
| - filter บ้าน    |
| - ตอบอัตโนมัติ  |
+-----------------+
          |
          v
+-----------------+
| ผู้ใช้เห็นผลลัพธ์ |
+-----------------+


💡 สรุปการทำงาน

    ผู้ใช้พิมพ์ข้อความ → ส่งไปให้ AI Intent Engine

    ได้ intent + keywords + embedding กลับมา

    ระบบสามารถ ตอบอัตโนมัติ หรือ ค้นหาข้อมูลตรงใจผู้ใช้


✅ ประโยชน์ของข้อมูลแต่ละส่วน

| ส่วนข้อมูล    | ประโยชน์                                       |
| ------------- | ---------------------------------------------- |
| **text**      | จำว่าผู้ใช้พิมพ์อะไร, แสดงผลอ้างอิง            |
| **intent**    | รู้ว่าผู้ใช้ต้องการอะไร → ทำงานต่ออัตโนมัติ    |
| **keywords**  | ช่วยกรองข้อมูลและค้นหาแม่นยำ                   |
| **embedding** | ค้นหาแบบ semantic → เข้าใจความหมายเหมือนมนุษย์ |
